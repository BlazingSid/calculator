
# Python Calculator

A simple command-line calculator written in Python.

## Features
- Evaluate arithmetic expressions (e.g., `2+2`, `3*4`, `(2+3)/4`)
- Supports power operator `**` and common math functions (`sqrt`, `sin`, `cos`, ...)
- Small, beginner-friendly code — great for learning Python

## How to run
1. Clone the repo:
   ```bash
   git clone https://github.com/BlazingSid/python-calculator.git
   cd python-calculator
